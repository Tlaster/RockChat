<ContentDialog
    x:Class="RockChat.UWP.Dialogs.ProxyDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:RockChat.UWP.Dialogs"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:Microsoft.UI.Xaml.Controls"
    mc:Ignorable="d"
    Title="Proxy"
    DefaultButton="Primary"
    PrimaryButtonText="Save"
    CloseButtonText="Close">
    <StackPanel Orientation="Vertical">
        <TextBox Header="Url" Text="{x:Bind Data.Url, Mode=TwoWay}"/>
        <TextBox Header="User Name (Optional)" Text="{x:Bind Data.UserName, Mode=TwoWay}"/>
        <TextBox Header="Password (Optional)" Text="{x:Bind Data.Password, Mode=TwoWay}"/>
    </StackPanel>
</ContentDialog>